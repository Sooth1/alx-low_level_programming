#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int c, l, k, j;

	for (c = 47; c <= 58; c++)
	{
		for (l = 47; l <= 58; l++)
		{
			for (k = 47; k <= 58; k++)
			{
				for (j = 47; j <= 58; j++)
				{
					if (((k + j) > (c + i) && k >= c) || c < k)
					{
						putchar(c);
						putchar(l);
						putchar('');
						putchar(k);
						putchar(j);
	if (c + i + k + j == 227 && c == 57)
					  {	  break;
						}
	else
						{
							putchar(',');
							putchar('');
						}
					}
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
